# SDM Manager GUI Requirements
# Core HTTP and async libraries
aiohttp>=3.8.0
requests>=2.28.0

# JWT token handling
PyJWT>=2.6.0

# GUI libraries (tkinter is built-in with Python)
# No additional GUI dependencies needed as we use tkinter

# Build and packaging
pyinstaller>=5.7.0

# Optional: For better error handling and logging
urllib3>=1.26.0

# Platform compatibility
setuptools>=65.0.0
wheel>=0.37.0